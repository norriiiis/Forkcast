// Browser stub for `@/lib/db`. The standalone single-file build runs the overlap
// engine on an embedded recipe pool and never queries the database, so Prisma is
// aliased to this empty stub at bundle time (see scripts/build-standalone.ts).
export const prisma = {} as never;
